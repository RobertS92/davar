# Davar Supabase project config (optional local CLI)
project_id = "davar"

[api]
enabled = true
port = 54321

[db]
port = 54322
major_version = 15

[auth]
enabled = true

[auth.email]
enable_signup = false

# Enable Anonymous sign-ins in the Supabase dashboard:
# Authentication → Providers → Anonymous → Enable
